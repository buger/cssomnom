id: INT-REQ-260821-30ZA
version: 1
status: review
priority: shall
category: interface
req_type: guarantee
fretish: when insert_rule_path & box_side_count <= 4 & position_token_count <= 4 & keyframe_offset_percent <= 100 & font_weight_number <= 1000 the cssom shall always satisfy parse_hooks_consume_rule_called & !parser_imported & four_longhands_assigned & (shorthand_expanded | shorthand_rejected)
description: The cssom insertRule path shall call ParseHooks.consumeRule. The cssom module shall not import Parser. Box shorthands stay within 1-4 sides and assign all four longhands.
formalization_strategy: fretish
informal_verification:
    method: ""
    evidence: ""
    verified: false
parent: SYS-REQ-260821-YMEY
component: cssom
rationale: CSSOM mutation shall use injected parse hooks and shall avoid a parser import cycle.
tags: []
variables:
    - insert_rule_path
    - box_side_count
    - position_token_count
    - keyframe_offset_percent
    - font_weight_number
    - parse_hooks_consume_rule_called
    - parser_imported
    - four_longhands_assigned
    - shorthand_expanded
    - shorthand_rejected
references:
    - type: document
      id: README.md
traces:
    satisfies:
        - SYS-REQ-260821-YMEY
    components:
        - cssom
        - parser
verification:
    assurance_level: C
    formalization_status: valid
    review:
        status: in_review
        reviewer: "REVIEW-54"
        reviewed_at: "2026-08-24T15:46:40Z"
        ai_generated: true
    not_modeled:
        - predicate: length <= 6
          reason: "CSS escaped-code-point hex digit cap (css-syntax max 6 hex digits); structural tokenizer helper, not a requirement domain [structural]"
        - predicate: tokens.length >= 2
          reason: "Function/qualified-rule arity guard on the token list; structural parse, not a requirement domain [structural]"
        - predicate: val > 100
          reason: "Incidental percent/number clamp in color or numeric parse; not a modeled requirement domain [structural]"
history:
    created_by: agent:grok-4.6
    created_at: "2026-08-21T12:36:47Z"
    last_modified_by: agent:champ
    last_modified_at: "2026-08-24T12:06:23Z"
obligation_checklist:
    - error_handling
    - malformed_input
obligation_hazards:
    - class: error_handling
      worst_case: If ParseHooks.consumeRule is not injected, insertRule must throw rather than import Parser or silently no-op; a missing throw hides a broken parse path.
      severity: high
    - class: malformed_input
      worst_case: insertRule that does not SyntaxError on consumeRule failure mutates cssRules with a bogus rule.
      severity: high
interface:
    caller: cssom
    callee: parser
    type: function_call
    signature: 'ParseHooks.consumeRule(tokens: Token[]) -> Rule'
    assumptions: "parser.ts injects ParseHooks.consumeRule at module load. CSSOM must not import Parser (circular-dependency inversion via ParseHooks)."
    guarantees: "CSSStyleSheet.insertRule calls ParseHooks.consumeRule. CSSOM.ts does not import Parser. Missing injection throws 'consumeRule not injected'."
    producer: parser
    consumer:
        - cssom
    owner: maintainer:conformance_owner
    version: 1.0.0
    compatibility_policy: breaking-allowed-with-migration
    risk_tier: 3
lifecycle:
    change_history:
        - date: "2026-08-21T12:39:06Z"
          from: draft
          to: review
          reason: 'Onboard skeleton believed: four-layer graph linked to research map'
          changed_by: agent:grok-4.6
