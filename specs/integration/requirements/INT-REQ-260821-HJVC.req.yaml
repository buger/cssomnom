id: INT-REQ-260821-HJVC
version: 1
status: review
priority: shall
category: interface
req_type: guarantee
fretish: when cascaded_style_requested the cascade shall always satisfy matcher_and_media_consulted
description: The cascade shall walk CSSOM rules and call matcher, MediaParser, and supports when a cascaded style is requested.
formalization_strategy: fretish
informal_verification:
    method: ""
    evidence: ""
    verified: false
parent: SYS-REQ-260821-ZXZW
component: cascade
rationale: getCascadedStyle shall consult selectors, media, and supports before it picks a winner.
tags: []
variables:
    - cascaded_style_requested
    - matcher_and_media_consulted
audit_ignore:
    - check: under_modeled_requirements_clean
      reason: "Intentionally coarse cascade-walk contract: matcher/MediaParser/supports consultation on getCascadedStyle; witnessed by tests/mcdc-witness-cascade.test.ts and tests/integration-int-req.test.ts. Cascade sorting math is owned by the SW cascade models rather than this interface clause."
references:
    - type: document
      id: README.md
traces:
    satisfies:
        - SYS-REQ-260821-ZXZW
    components:
        - cascade
        - cssom
verification:
    assurance_level: C
    formalization_status: valid
    review:
        status: in_review
        reviewer: "REVIEW-54"
        reviewed_at: "2026-08-24T15:46:40Z"
        ai_generated: true
    not_modeled:
        - predicate: h < 120
          reason: "HSL hue-sector lookup table bound for color conversion; not a cascade/CSSOM contract domain [structural]"
        - predicate: h < 180
          reason: "HSL hue-sector lookup table bound for color conversion; not a cascade/CSSOM contract domain [structural]"
        - predicate: h < 240
          reason: "HSL hue-sector lookup table bound for color conversion; not a cascade/CSSOM contract domain [structural]"
        - predicate: h < 300
          reason: "HSL hue-sector lookup table bound for color conversion; not a cascade/CSSOM contract domain [structural]"
        - predicate: h < 60
          reason: "HSL hue-sector lookup table bound for color conversion; not a cascade/CSSOM contract domain [structural]"
        - predicate: nonEofTokens.length < 3
          reason: "Selector/color component-list arity guard; structural parse, not a requirement domain [structural]"
        - predicate: parts.length < 3
          reason: "rgb()/color() slash-separated parts arity guard; structural parse, not a requirement domain [structural]"
        - predicate: parts.length > 4
          reason: "rgb()/color() slash-separated parts arity guard; structural parse, not a requirement domain [structural]"
history:
    created_by: agent:grok-4.6
    created_at: "2026-08-21T12:36:48Z"
    last_modified_by: agent:grok-4.6
    last_modified_at: "2026-08-22T10:44:42Z"
obligation_checklist:
    - malformed_recovers_or_errors_loudly
obligation_hazards:
    - class: malformed_recovers_or_errors_loudly
      worst_case: Cascade that throws on unmatched selectors or invalid @media/@supports aborts style resolution instead of skipping those rules.
      severity: medium
interface:
    caller: cascade
    callee: cssom
    type: function_call
    signature: getCascadedStyle(element, rules) walks CSSRule; matches(element, selector); MediaParser.evaluate(mediaText); supports(conditionText)
    assumptions: "getCascadedStyle is given CSSOM rules and a DOM-like element. While walking, cascade calls matches() (selectors), MediaParser.evaluate() (media), and parser_api supports() — not CSS.supports sidecar."
    guarantees: "An unmatched selector that would win if applied does not win. A later same-specificity rule inside @media not all does not win. A later failing @supports does not win; a passing @supports can change the color."
    producer: cssom
    consumer:
        - cascade
    owner: maintainer:conformance_owner
    version: 1.0.0
    compatibility_policy: semver-lax
    risk_tier: 2
lifecycle:
    change_history:
        - date: "2026-08-21T12:39:07Z"
          from: draft
          to: review
          reason: 'Onboard skeleton believed: four-layer graph linked to research map'
          changed_by: agent:grok-4.6
