id: SW-REQ-260821-6951
version: 1
status: review
priority: shall
category: functional
req_type: guarantee
fretish: when css_rules_getter_runs the cssom shall always satisfy (origin_clean & !security_error_thrown) | (!origin_clean & security_error_thrown)
description: The cssom cssRules getter shall throw SecurityError when origin-clean is false. The cssom shall not throw SecurityError when origin-clean is true.
formalization_strategy: fretish
informal_verification:
    method: ""
    evidence: ""
    verified: false
parent: SYS-REQ-260821-X3KX
component: cssom
rationale: The cssRules accessor shall enforce the origin-clean flag.
tags: []
variables:
    - css_rules_getter_runs
    - origin_clean
    - security_error_thrown
references:
    - type: document
      id: README.md
traces:
    satisfies:
        - SYS-REQ-260821-X3KX
verification:
    assurance_level: C
    formalization_status: valid
    review:
        status: in_review
        reviewer: agent:grok-4.6
        reviewed_at: "2026-08-21T13:53:27Z"
        ai_generated: true
    not_modeled:
        - predicate: 2 < tokens.length
          reason: "Shorthand slash-split / component-list arity guard; structural CSS value parse, not an origin-clean domain [structural]"
        - predicate: data.length > 2
          reason: "Shorthand box/radius component-list arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: data.length > 3
          reason: "Shorthand box/radius component-list arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: data.length > 4
          reason: "Shorthand box/radius component-list arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: filtered.length > 2
          reason: "Shorthand filtered-token arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: filtered.length > 3
          reason: "Shorthand filtered-token arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: hValues.length > 4
          reason: "border-radius horizontal value-list arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: i < 4
          reason: "DOMMatrix 4x4 index loop bound inherited via CSSOM Implements; structural linear-algebra helper, not an origin-clean domain [structural]"
        - predicate: positionTokens.length > 4
          reason: "background-position token-list arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: token.value <= 1000
          reason: "Incidental font-weight numeric cap in shorthand expand; not an origin-clean contract domain [structural]"
        - predicate: vValues.length > 4
          reason: "border-radius vertical value-list arity guard; structural parse, not an origin-clean domain [structural]"
        - predicate: val > 100
          reason: "Incidental percent/number clamp in color or numeric parse; not a modeled requirement domain [structural]"
history:
    created_by: agent:grok-4.6
    created_at: "2026-08-21T12:35:57Z"
    last_modified_by: agent:grok-4.6
    last_modified_at: "2026-08-22T14:00:00Z"
obligation_checklist:
    - access_denied
    - error_handling
obligation_hazards:
    - class: access_denied
      worst_case: cssRules getter that skips the origin-clean check returns foreign-sheet rules, leaking stylesheet contents across origins.
      severity: high
    - class: error_handling
      worst_case: cssRules getter that throws a TypeError or returns [] on a tainted sheet hides the origin-clean violation as a benign empty list.
      severity: high
lifecycle:
    change_history:
        - date: "2026-08-21T12:39:07Z"
          from: draft
          to: review
          reason: 'Onboard skeleton believed: four-layer graph linked to research map'
          changed_by: agent:grok-4.6
