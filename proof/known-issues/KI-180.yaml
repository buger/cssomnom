id: KI-180
title: '#resolveVarFunction splices a malformed var() away instead of poisoning the value'
description: '#resolveVarFunction returns an empty component-value array when the var() first argument is not a dashed ident (src/parser.ts:1752-1757). #resolveVariablesInComponentValues spreads that array, so neighboring tokens survive. css-variables-1 #guaranteed-invalid makes an invalid substitution function poison the containing property at computed-value time. Parser.resolveVariables returns ''red blue'' for ''red var(foo) blue'' instead of the empty invalid signal. The env() sibling of this splice is KI-174.'
affected_requirements:
    - INT-REQ-260821-N2VE
evidence_manifests:
    - proof/evidence/ki-180.yaml
severity: medium
severity_basis: risk
risk: correctness
affected_api: Parser.resolveVariables malformed var() substitution
attacker_input: stylesheet declaration mixing valid tokens with a var() whose name is not a dashed ident; local library input, no trust boundary crossed
sanitizer: manual:logic
reproducer_command: /opt/node24/bin/node --experimental-strip-types --test proof/reproducers/CRS-0007-var-malformed-spliced.ts
introduced_in: inception
defect_class: latent
minimization_status: minimized
mitigation: Avoid malformed var() functions in values until substitution poisons the whole declaration.
remediation: Return the guaranteed-invalid token for a var() that fails the grammar so the containing value resolves to the empty invalid signal.
status: open
history:
    - at: "2026-09-03T21:53:37Z"
      by: human:Leonid Bugaev
      action: created
    - at: "2026-09-03T21:57:28Z"
      by: human:Leonid Bugaev
      action: capture-evidence
      detail: proof/evidence/ki-180.yaml
source_claim: CRS-0007/C06
