id: KI-129
title: StylePropertyMap.append skips the pending-substitution TypeError when var() hides behind a shorthand
description: 'StylePropertyMap.append stores values silently when the target longhand carries a var() reference inherited from a shorthand declaration. css-typed-om-1 #append-to-a-stylepropertymap step 7 throws TypeError when the stored entry contains a var() reference, before any mutation. A var() held by transition keeps every derived longhand carrying the reference. Local WPT declared append.tentative.html pins the throw for the transition var a case. The same append against an own-text var() reference throws today, so the guard exists but reads only the property text.'
affected_requirements:
    - SYS-REQ-260825-2FMA
commands:
    - /opt/node24/bin/node --experimental-strip-types --test proof/reproducers/KI-129-append-pending-substitution-shorthand-overlay-260825.ts
evidence_manifests:
    - proof/evidence/ki-129.yaml
severity: medium
severity_basis: reproducer
risk: correctness
cve_surface: none
affected_api: StylePropertyMap.append() pending-substitution guard over shorthand-expanded declarations
attacker_input: ordinary shorthand declarations containing var() followed by Typed OM append calls; no attacker required
sanitizer: manual:logic
reproducer_command: /opt/node24/bin/node --experimental-strip-types --test proof/reproducers/KI-129-append-pending-substitution-shorthand-overlay-260825.ts
tripwire_mutation: Extend the pending-substitution check to shorthand-derived entries so both defect legs throw while the longhand control and the plain-append control stay unchanged
introduced_in: inception
defect_class: latent
minimization_status: minimized
mitigation: Consumers can probe the shorthand cssText for var() themselves before calling append on derived longhands.
remediation: 'Make the append guard resolve shorthand-expanded storage and throw TypeError per #append-to-a-stylepropertymap step 7 without mutating state.'
owner: agent:ox-alpha
review_date: "2026-09-25"
release_disposition: ship_with_known_issue
status: open
history:
    - at: "2026-08-25T13:47:12Z"
      by: agent:champ
      action: created
    - at: "2026-08-25T13:47:49Z"
      by: human:Leonid Bugaev
      action: refresh-evidence
      detail: proof/evidence/ki-129.yaml
poc_quality:
    real_infrastructure: true
    quantitative_impact: true
    control_test: true
    untrusted_attacker: false
    current_deployment_relevance: true
    honest_severity: true
    attacker_profitability: false
    reproducible: true
    no_inflation: true
    operational_path_verified: true
    reachability_verified: true
    notes: |
        real_infrastructure: public StylePropertyMap.append over shorthand-derived longhand storage - no mocks; scenario mirrors local WPT declared append.tentative.html.
        quantitative_impact: measured at HEAD - append on transition-duration after transition var a stores silently (shorthand-hidden leg) and the second shorthand-hidden leg behaves identically, while the same append against an own-text var() reference throws today.
        control_test: two dedicated controls - the own-text longhand throw proves the guard exists, and the plain append without var() stays green - isolate the escape to shorthand-derived entry resolution.
        untrusted_attacker: ordinary shorthand declarations containing var() followed by Typed OM append calls are the input surface.
        current_deployment_relevance: pending-substitution values are ubiquitous in real stylesheets; the silent store corrupts Typed OM state at HEAD.
        honest_severity: medium correctness - step 7 orders the TypeError before mutation, so silent storage breaks both the error contract and state integrity.
        attacker_profitability: not profit-driven; skipped throws let substitution-pending garbage enter typed maps.
        reproducible: node --experimental-strip-types --test proof/reproducers/KI-129-append-pending-substitution-shorthand-overlay-260825.ts exits 1 deterministically across runs and evidence capture.
        no_inflation: severity medium matches the guard-gap blast radius; set() replacement behind shorthands and KI-107/KI-108/KI-109 var() surfaces stay separate filings; cve_surface none.
        operational_path_verified: failing legs drive the real append guard path over stored declaration entries.
        reachability_verified: public StylePropertyMap IS the product surface; no gating layer exists between caller arguments and entry mutation.
kill_domain: pending_substitution_guard_gap
do_not_refile_as: 'var() feature-query grammar (KI-107), substitution order in cascade (KI-108), case-insensitive dispatch (KI-109): different surfaces; set() replacement behind shorthands: no spec clause mandates a throw there'
